{
    "compilerOptions": {
        "baseUrl": ".", // 상대 경로 기준
        "paths": {
            "@/*": [
                "src/*",
                "components/*",
                "screens/*"
            ] // ✅ src와 components 모두 인식하도록 설정
        },
        "jsx": "react-native"
    },
    "extends": "expo/tsconfig.base"
}